// export default function changeStatus(name, id) {
//   if (
//     name === "In Progress" &&
//     !inProgress.some(todo => todo._id === id)
//   ) {
//     setInProgress([...inProgress, { _id: id, title, description }]);
//     setCurrentTodos(prevCopy =>
//       prevCopy.filter(todo => todo._id !== id)
//     );
//   } else if (name === "Completed" && !completed.some(todo => todo._id === id)) {
//     setCompleted([...inProgress, { _id: id, title, description }]);
//     setCurrentTodos(prevCopy =>
//       prevCopy.filter(todo => todo._id !== id)
//     );
//   }

// }
